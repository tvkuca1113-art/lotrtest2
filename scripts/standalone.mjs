import {readFileSync,readdirSync,writeFileSync,mkdirSync} from 'node:fs';
import {resolve} from 'node:path';
const root='public/assets',assets={};const walk=(dir,prefix='')=>{for(const entry of readdirSync(dir,{withFileTypes:true})){const path=dir+'/'+entry.name,key=prefix+entry.name;if(entry.isDirectory())walk(path,key+'/');else{const ext=entry.name.split('.').pop(),mime={png:'image/png',webp:'image/webp',wav:'audio/wav',json:'application/json'}[ext];if(mime)assets['assets/'+key]='data:'+mime+';base64,'+readFileSync(path).toString('base64');}}};walk(root);
const js=readFileSync('.standalone-build/game.js','utf8').replace(/(?:\.\/|\/)?assets\/title\.webp/g,assets['assets/title.webp']);const cssFile=readdirSync('.standalone-build').find(n=>n.endsWith('.css'));let css=readFileSync(cssFile?'.standalone-build/'+cssFile:'src/style.css','utf8');css=css.replace(/url\([^)]*title\.webp[^)]*\)/g,`url('${assets['assets/title.webp']}')`);
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,viewport-fit=cover"><meta name="theme-color" content="#171c1b"><title>The Last Hearth · Rings of the North</title><style>${css}</style></head><body><div id="game" aria-label="Isometric action role-playing game"></div><div id="ui"></div><div id="announcer" role="status" aria-live="polite"></div><script>window.__HEARTH_ASSETS__=${JSON.stringify(assets)};</script><script>${js.replace(/<\/script/gi,'<\\/script')}</script></body></html>`;
mkdirSync('release',{recursive:true});writeFileSync('release/The-Last-Hearth.html',html);console.log('Standalone game: '+(Buffer.byteLength(html)/1024/1024).toFixed(2)+' MiB; '+Object.keys(assets).length+' embedded assets.');
